option batch on
option confirm off

open skyesoft-godaddy

cd /public_html

synchronize remote "." "." 
  -exclude ".git/*"
  -exclude ".env"
  -exclude "secure/*"
  -exclude "node_modules/*"
  -exclude "repoTree.txt"
  -exclude "tree.txt"

exit
