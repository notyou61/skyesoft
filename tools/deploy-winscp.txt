option batch on
option confirm off

open sftp://notyou64:Steven1!@68.178.221.71:22 -hostkey="ssh-ed25519 255 9NktM0x2LybydB83Ie/azLpT7VpKQIpMfjfouXmVZWY"

# Change to the repo root (passed as %1% from PowerShell)
lcd "%1%"

# Move to the parent folder on remote where skyesoft should live
cd /home/notyou64/public_html

# === DEPLOYMENT ===
# Push contents of current local dir (.) → remote subfolder 'skyesoft'
#
# -delete
#     Remove remote files no longer present locally (true mirror)
#
# -filemask
#     Exclude folders/files from deployment
#
# Excluded:
#     .git
#     node_modules
#     secure
#     .env
#     repoTree.txt
#     tree.txt
#     authoritative jurisdiction datasets
#     vendor libraries (static Composer packages)

synchronize remote -delete -filemask="|.git/;node_modules/;secure/;.env;repoTree.txt;tree.txt;data/authoritative/jurisdictions/;vendor/" . skyesoft

# === PREVIEW MODE (SAFE TESTING) ===
# Uncomment below and comment out live deploy above for dry-run preview

# synchronize remote -preview -delete -filemask="|.git/;node_modules/;secure/;.env;repoTree.txt;tree.txt;data/authoritative/jurisdictions/;vendor/" . skyesoft

exit